# Portfolio
Personal Portfolio

## Search engine indexing

The site includes `public/robots.txt` which disallows all crawlers from indexing the site. Note that some crawlers may ignore `robots.txt`; if you need stronger protection, consider adding authentication or access controls at the host (Vercel) level.

